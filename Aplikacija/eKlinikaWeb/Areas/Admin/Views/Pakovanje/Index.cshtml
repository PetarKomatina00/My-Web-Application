@model IEnumerable<Pakovanje>
@{
    ViewData["Title"] = "Index";
}


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Lista pakovanja</h2>
		</div>
        <div class="col-6 text-end">
            <a asp-controller="Pakovanje" asp-action="Create" class="btn btn-primary">
               <i class="bi bi-plus-circle"></i> &nbsp; Kreiraj pakovanje
			</a>
		</div>
	</div>
    <br /><br />

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Vrsta Pakovanja
			</th>
            
		</tr>
	</thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="50%">
                    @obj.VrstaPakovanja
				</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Pakovanje" asp-action="Edit" asp-route-id="@obj.PakovanjeID"
                        class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Izmeni</a>
                        <a asp-controller="Pakovanje" asp-action="Delete" asp-route-id="@obj.PakovanjeID"
                        class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Obriši</a>
					</div>
				</td>
			</tr>
        }
    </tbody>

</table>
</div>